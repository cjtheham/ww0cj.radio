{% for post in postslist | reverse %}
<span><i>{{ post.date | moYear }}</i> <a href="{{ post.url }}" class="postlist-link">{% if post.data.title %}{{ post.data.title }}{% else %}<code>{{ post.url }}</code>{% endif %}</a></span>
{% endfor %}